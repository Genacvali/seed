# SberDevices Alertmanager Integration Configuration
# Copy this to your environment or source it before running the integration

# SberDevices Alertmanager API
SBER_ALERTMANAGER_URL=https://alertmanager.sberdevices.ru
SBER_AUTH_TOKEN=your-auth-token-here

# SEED Agent Configuration
SEED_AGENT_URL=http://localhost:8080

# Integration Settings
SYNC_INTERVAL=60  # seconds between alert fetches

# For webhook mode (if configuring Alertmanager to push to SEED)
SEED_WEBHOOK_USER=seed-agent
SEED_WEBHOOK_PASSWORD=your-webhook-password-here