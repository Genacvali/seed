# 🔌 SEED Agent - Offline Deployment Guide\n\n**Развертывание SEED Agent на VM без доступа к интернету**\n\n## 📋 Обзор\n\nДля развертывания SEED Agent на изолированных VM без интернета необходимо:\n1. Подготовить Docker образы на машине с интернетом\n2. Перенести образы на целевую VM\n3. Загрузить образы и запустить систему\n\n## 🏗️ Подготовка (машина с интернетом)\n\n### 1. Сохранение Docker образов\n\n```bash\ncd v3/\n\n# Автоматическое сохранение всех необходимых образов\nmake save-images\n\n# Или вручную:\n./save-images.sh\n```\n\n### 2. Проверка сохраненных образов\n\n```bash\n# Проверить размеры файлов\nls -lh docker-images/\n\n# Примерные размеры:\n# rabbitmq.tar     ~180MB - RabbitMQ с Management UI\n# redis.tar        ~30MB  - Redis 7.2 Alpine\n# python.tar       ~120MB - Python 3.11 Slim\n# prometheus.tar   ~200MB - Prometheus (опционально)\n# grafana.tar      ~300MB - Grafana (опционально)\n```\n\n### 3. Подготовка к переносу\n\n```bash\n# Создать архив всей директории v3\ntar -czf seed-agent-v3.tar.gz v3/\n\n# Или скопировать только необходимые файлы\ncp -r v3/ /path/to/transfer/\n```\n\n## 🚀 Развертывание (целевая VM)\n\n### Предварительные требования\n\n- Docker установлен на целевой VM\n- Docker Compose установлен\n- Достаточно места на диске (минимум 1GB)\n\n### 1. Перенос файлов\n\n```bash\n# Распаковать архив\ntar -xzf seed-agent-v3.tar.gz\n\n# Или скопировать директорию\nscp -r v3/ user@target-vm:/opt/seed/\n```\n\n### 2. Загрузка образов и запуск\n\n```bash\ncd v3/\n\n# Полная автоматическая установка\nmake offline-setup\n\n# Или пошагово:\nmake load-images              # Загрузить образы\nmake build                    # Собрать SEED Agent\nmake up                       # Запустить систему\n```\n\n### 3. Проверка развертывания\n\n```bash\n# Проверить статус сервисов\ndocker-compose ps\n\n# Проверить здоровье системы\nmake health\n\n# Проверить логи\nmake logs\n```\n\n## 🔧 Конфигурация для offline\n\n### Отключение внешних зависимостей\n\nОтредактируйте `seed.yaml` для работы в изолированной сети:\n\n```yaml\n# Отключить внешние уведомления если нет доступа\nnotifications:\n  mattermost:\n    enabled: false  # Отключить если Mattermost недоступен\n  slack:\n    enabled: false  # Отключить если Slack недоступен\n  email:\n    enabled: true   # Можно оставить для внутренней почты\n    smtp_host: \"internal-smtp.company.com\"\n\n# Настроить для внутренних хостов\nhosts:\n  groups:\n    internal-servers:\n      hosts: [\"server-01\", \"server-02\", \"server-03\"]\n      overrides:\n        telegraf_url: \"http://{}:9216/metrics\"\n```\n\n## 📊 Мониторинг и обслуживание\n\n### Проверка системы\n\n```bash\n# Статус всех сервисов\nmake health-all\n\n# Статистика обработки алертов\ncurl http://localhost:8080/stats\n\n# Доступность RabbitMQ Management\n# http://localhost:15672 (seed/seed123)\n```\n\n### Обновление конфигурации\n\n```bash\n# Изменить seed.yaml или plugins.py\nvim seed.yaml\nvim plugins.py\n\n# Перезагрузить конфигурацию без перезапуска\nmake reload-config\n```\n\n### Резервное копирование\n\n```bash\n# Создать резервную копию\nmake backup\n\n# Архивировать всю конфигурацию\ntar -czf seed-backup-$(date +%Y%m%d).tar.gz seed.yaml plugins.py logs/\n```\n\n## 🐛 Troubleshooting\n\n### Проблемы с образами\n\n```bash\n# Проверить загруженные образы\ndocker images | grep -E \"(rabbitmq|redis|seed)\"\n\n# Перезагрузить образ\ndocker load -i docker-images/rabbitmq.tar\n\n# Удалить поврежденные образы\ndocker image prune -f\n```\n\n### Проблемы с сетью\n\n```bash\n# Проверить Docker сеть\ndocker network ls | grep seed\n\n# Проверить подключение между контейнерами\ndocker-compose exec seed-agent ping rabbitmq\ndocker-compose exec seed-agent ping redis\n```\n\n### Проблемы с конфигурацией\n\n```bash\n# Проверить синтаксис YAML\npython -c \"import yaml; yaml.safe_load(open('seed.yaml'))\"\n\n# Проверить плагины\npython plugins.py list\npython plugins.py test host_inventory\n```\n\n### Проблемы с производительностью\n\n```bash\n# Проверить использование ресурсов\ndocker stats\n\n# Увеличить лимиты памяти в docker-compose.yml\n# Оптимизировать TTL алертов в seed.yaml\n```\n\n## 📋 Контрольный список развертывания\n\n- [ ] Docker и Docker Compose установлены\n- [ ] Образы сохранены и перенесены (`docker-images/`)\n- [ ] Конфигурация адаптирована для локальной сети (`seed.yaml`)\n- [ ] Плагины настроены для ваших хостов (`plugins.py`)\n- [ ] Система запущена (`make offline-setup`)\n- [ ] Все сервисы работают (`make health-all`)\n- [ ] Тестовый алерт обработан успешно\n- [ ] Мониторинг работает (RabbitMQ UI доступен)\n- [ ] Резервное копирование настроено (`make backup`)\n\n## 🎯 Следующие шаги\n\n1. **Настройка мониторинга**: Добавьте ваши хосты в `seed.yaml`\n2. **Интеграция алертов**: Настройте Alertmanager для отправки в SEED\n3. **Кастомные плагины**: Добавьте специфичную логику в `plugins.py`\n4. **Автозапуск**: Настройте systemd service для автостарта\n5. **Мониторинг метрик**: При необходимости включите Prometheus/Grafana\n\n---\n\n**Развертывание в изолированной среде завершено!** 🎉